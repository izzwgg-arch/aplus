# Smart Steps ABA Tracker – Nginx proxy (add to your existing site or create a new server block)
# Proxy /smart-steps to the Next.js app on port 3001 so dashboard can use same-origin SSO.

# Inside your server { } block for app.apluscentercinc.org (or your domain):
#
#   location /smart-steps {
#     proxy_pass http://127.0.0.1:3001;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection 'upgrade';
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_cache_bypass $http_upgrade;
#   }
#
# Then: sudo nginx -t && sudo systemctl reload nginx
